import os;
import pyautogui as pag;
import time;
import subprocess;
#opens the google chrome app
time.sleep(15),
#moves to the join button on the same page
pag.moveTo(1168,616,1),
pag.click(1158,616,1),
time.sleep(10),
#moves to the join now button on the classroom page
pag.moveTo(900, 532,1),
pag.click(900,532,1),
time.sleep(2);
